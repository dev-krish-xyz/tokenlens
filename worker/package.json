{
  "name": "@tokenlens/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tokenlens/shared": "workspace:*",
    "bullmq": "^5.56.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "*"
  }
}
