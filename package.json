{
  "name": "tokenlens",
  "private": true,
  "workspaces": ["gateway", "web", "worker", "packages/*", "scripts"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "deploy:web": "bunx vercel@latest --cwd web"
  },

  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.8.3"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.2"
  }
}
